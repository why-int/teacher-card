# Иконки приложения

Можно положить сюда иконку для сборки:

- `app_icon.ico` для Windows
- `app_icon.icns` для macOS
- или `app_icon.png` (PyInstaller попробует конвертировать)

Если иконки нет, приложение всё равно соберётся и будет запускаться с системной иконкой.
